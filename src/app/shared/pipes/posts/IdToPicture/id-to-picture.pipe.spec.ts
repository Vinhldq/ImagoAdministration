import { IdToPicturePipe } from './id-to-picture.pipe';

describe('IdToPicturePipe', () => {
  it('create an instance', () => {
    const pipe = new IdToPicturePipe();
    expect(pipe).toBeTruthy();
  });
});
